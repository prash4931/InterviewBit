Now cosider the following code snippet and write it’s output:

function fun1(a) {
  a *= 2;
  function fun2(b) {
    return a + b; 
  } 
  a += 2;
  return fun2; 
}

function fun3(){
  return fun1(2) (2);
}

console.log(fun3());

//8
